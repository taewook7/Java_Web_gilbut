10월3일 공부내용
스프링 API 를 이용한 AOP 기능 구현 과정
1. 타깃(target) 클래스 지정
2. 어드바이스(advice) 클래스 지정
3. 설정 파일에서 포인트컷(pointcut)을 지정
4. 설정파일에서 어드바이스와 포인트컷을 결합하는 어드바이저를 설정
5. 설정파일에서 스프링의 proxyfactorybean 클래스를 이용해 타깃에 어드바이스를 설정합니다.
6. getbean() 메서드로 빈 객체에 접근해 사용한다.


AOP 기능 실습 

